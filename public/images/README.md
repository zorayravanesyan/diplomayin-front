# Images folder

- **logo.svg** — header logo (sports/health themed).
- Banner and about images are loaded from `src/data/images.js` (Unsplash URLs by default).
- To use local files: add images here (e.g. `banner-training.jpg`, `about-food.jpg`) and update paths in `src/data/images.js` to `/images/...`.

Themes: sports, health, healthy eating, healthy sleep, training.
